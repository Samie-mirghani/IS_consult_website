<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Insights & Solutions Consulting</title>
  <style>
    body {
      margin: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #2B4570;
      color: white;
    }
    .hero-section {
      padding: 100px 20px;
      text-align: center;
      animation: fadeInBackground 2s ease-in-out forwards;
    }

    .hero-title {
      color: white;
      font-size: 2.8rem;
      font-weight: 700;
      margin-bottom: 20px;
      opacity: 0;
      animation: fadeInUp 1.5s ease-out forwards;
      animation-delay: 0.5s;
    }
    .hero-description {
      font-size: 1.2rem;
      max-width: 750px;
      margin: 0 auto 30px;
      opacity: 0;
      animation: fadeInUp 1.5s ease-out forwards;
      animation-delay: 1.2s;
    }

    .button-container {
      display: flex;
      justify-content: center;
      gap: 16px;
      opacity: 0;
      animation: fadeInUp 1.5s ease-out forwards;
      animation-delay: 2s;
    }

    .button {
      width: 300px;
      height: 50px;
      box-sizing: border-box;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: bold;
      border-radius: 5px;
      text-decoration: none;
      font-size: 1rem;
      transition: background 0.3s ease, transform 0.3s ease;
      cursor: pointer;
    }
    .cta-button {
      background-color: #6DBE45;
      color: #333333;
    }
    .cta-button:hover {
      background-color: #5AAE3A;
      transform: scale(1.05);
    }
    .about-us-button {
      background-color: transparent;
      color: white;
      border: 2px solid white;
    }
    .about-us-button:hover {
      background-color: white;
      color: #2B4570;
      transform: scale(1.05);
    }

    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    @keyframes fadeInBackground {
      from { opacity: 0; }
      to { opacity: 1; }
    }

    @media (max-width: 600px) {
      .hero-section {
        padding: 50px 20px;
      }
      .hero-title {
        font-size: 2rem;
      }
      .hero-description {
        font-size: 1rem;
      }
      .button-container {
        flex-direction: column;
        align-items: center;
        gap: 12px;
      }
      .button {
        width: 100%;
        max-width: 300px;
      }
    }
  </style>
</head>
<body>

  <section class="hero-section">
    <h1 class="hero-title">Empowering Local Expertise to Solve Global Challenges</h1>
    <p class="hero-description">
      Insights & Solutions Consulting delivers expert assessments, strategic research, planning, and governance support—partnering with local leaders to drive sustainable, cross-sector impact in international development.
    </p>

    <div class="button-container">
      <a href="contact-us" class="button cta-button">
        Work With Us
      </a>
      <a href="#services" class="button about-us-button" onclick="document.querySelector('.what-we-do-section').scrollIntoView({ behavior: 'smooth' });">
        Our Services
      </a>
    </div>
  </section>

</body>
</html>
